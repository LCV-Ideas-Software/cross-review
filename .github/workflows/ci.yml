# This workflow is managed by gh actions-lock.

name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  merge_group:
    types:
      - checks_requested
# No workflow-level grant: each job declares its own minimal token scope.
permissions: {}
env:
  NPM_CLI_VERSION: "12.0.2"
  NPM_CLI_SHA512: "b885e890b9418fa1693544d05f53e64f9a73ec194837d4258b15fecdd692347b1dd2a517b1b0cbaf9d31cd8e92c3b70956bd2ecc72833a57b4b3098f5bfa7943"
jobs:
  verify:
    # checkout-only job: the token is used solely to fetch the repository.
    permissions:
      contents: read
    name: Build, lint and smoke
    environment: ci-automation
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Setup Node.js 24
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          check-latest: true
          package-manager-cache: false
      - name: Setup Python 3.12
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version-file: ".python-version"
      - name: Verify hash-locked Python tools and pre-commit config
        run: |
          python -m pip install --disable-pip-version-check --require-hashes -r python-tools-requirements.txt
          python scripts/validate-dependabot-config.py
          python -m pre_commit validate-config .pre-commit-config.yaml
          python -m pre_commit run --all-files --show-diff-on-failure
      - name: Setup hash-verified npm v12 toolchain
        uses: $/.github/actions/setup-npm-toolchain
        with:
          version: ${{ env.NPM_CLI_VERSION }}
          sha512: ${{ env.NPM_CLI_SHA512 }}
      - name: Verify npm v12 toolchain
        shell: bash
        run: |
          set -euo pipefail
          ACTUAL_NPM_VERSION="$(npm --version)"
          if [ "$ACTUAL_NPM_VERSION" != "$NPM_CLI_VERSION" ]; then
            echo "::error::Expected npm $NPM_CLI_VERSION, got $ACTUAL_NPM_VERSION."
            exit 1
          fi
          echo "Verified npm $ACTUAL_NPM_VERSION for npm v12 policy."
      - name: Install dependencies
        run: npm ci --strict-allow-scripts --no-audit --no-fund
      - name: Verify npm v12 CI and release policy
        run: npm run npm-v12-release-security-regression
      - name: Format check (prettier)
        run: npm run format:check
      - name: Lint (eslint)
        run: npm run lint
      - name: Biome (lint + format)
        run: npm run biome
      - name: Typecheck
        run: npm run typecheck
      - name: Run the complete regression suite
        run: npm test
        env:
          CROSS_REVIEW_STUB: "1"
          # v2.4.0 / P1.1 fail-fast: stub activation requires explicit
          # confirmation. The smoke harness is a legitimate stub consumer.
          CROSS_REVIEW_STUB_CONFIRMED: "1"
    timeout-minutes: 45
  caller-token-acl-windows:
    # checkout-only job: the token is used solely to fetch the repository.
    permissions:
      contents: read
    name: Windows caller-token ACL regression
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Setup Node.js 24
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          check-latest: true
          package-manager-cache: false
      - name: Setup hash-verified npm v12 toolchain
        uses: $/.github/actions/setup-npm-toolchain
        with:
          version: ${{ env.NPM_CLI_VERSION }}
          sha512: ${{ env.NPM_CLI_SHA512 }}
      - name: Install dependencies
        run: npm ci --strict-allow-scripts --no-audit --no-fund
      - name: Verify interruption-tolerant caller-token ACL recovery
        run: npm run v4.5.37-caller-token-acl-regression
    timeout-minutes: 15
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  queue: max
  cancel-in-progress: false
